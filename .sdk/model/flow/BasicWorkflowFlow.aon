# BasicWorkflowFlow

main: kit: flow: BasicWorkflowFlow:
{
  entity: "workflow",
  kind: "basic",
  name: "BasicWorkflowFlow",
  step: [
    {
      input: {
        ref: "workflow_ref01",
        srcdatavar: "workflow_ref01_data",
        suffix: "_dt0"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-workflow_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicWorkflowFlow"
}